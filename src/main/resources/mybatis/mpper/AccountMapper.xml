<?xml version="1.0" encoding="UTF-8" ?>
        <!DOCTYPE mapper
                PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="zl.mybatis.best.practice.mapper.AccountMapper">

        <insert id="insert" useGeneratedKeys="true" keyProperty="accountId">
                insert into account (name,nick_name,email,phone) value (#{name},#{nickName},#{email},#{phone})
        </insert>
</mapper>